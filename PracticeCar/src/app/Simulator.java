package app;

// create a class called Simulator w/ a main()
public class Simulator {
    public static void main(String[] args) throws Exception {
        
        // create an instance of a Car object 
        Car car1 = new Car();

        // invoke object's run() method
        car1.run();
    }
}